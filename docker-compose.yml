services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: ecopass
      POSTGRES_PASSWORD: ecopass
      POSTGRES_DB: ecopass
